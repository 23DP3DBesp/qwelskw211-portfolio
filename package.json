{
  "name": "qwelskw-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/clean.mjs && vite build && node scripts/build.mjs",
    "dev": "node scripts/dev.mjs",
    "db:generate": "drizzle-kit generate",
    "test": "node --test tests/*.test.mjs",
    "predev": "npm run build",
    "pretest": "npm run build"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "0.2.0",
    "drizzle-kit": "0.31.10",
    "drizzle-orm": "0.45.2",
    "miniflare": "^4.0.0",
    "vite": "^8.0.0"
  }
}
