'use strict';
const inquiryForm=document.querySelector('#inquiry-form');let submitted=false;
inquiryForm.addEventListener('submit',async event=>{event.preventDefault();if(submitted)return;const button=inquiryForm.querySelector('[type="submit"]'),status=document.querySelector('#inquiry-status');button.disabled=true;status.textContent='Sending…';try{const data=Object.fromEntries(new FormData(inquiryForm));const response=await fetch('/api/inquiries',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!response.ok)throw new Error();submitted=true;inquiryForm.reset();status.textContent='Request sent. I’ll get back to you using the contact you provided.';}catch{status.textContent='Could not send. Please try again or contact me directly.';button.disabled=false;}});
